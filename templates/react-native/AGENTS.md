# Agent Instructions

**Project**: Expo (managed) + React Native mobile starter — Expo Router (file-based,
typed routes), NativeWind v4, TanStack React Query, Zustand, an axios service layer
with JWT bearer + `expo-secure-store` refresh-token rotation and HMAC-signed
requests, react-i18next. Package manager: **pnpm**.

Project-specific guidance lives in [`CLAUDE.md`](./CLAUDE.md) and [`docs/`](./docs/README.md).
