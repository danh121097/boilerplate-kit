# Expo / React Native
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native builds (generated by `expo prebuild` — not committed for managed workflow)
/ios/
/android/

# Metro
.metro-health-check*

# Debug
npm-debug.*
*.log

# macOS
.DS_Store
*.pem

# Lock files — the template ships PM-agnostic; commit yours after scaffolding.
pnpm-lock.yaml
package-lock.json
yarn.lock
bun.lockb

# env files
.env
.env.*
!.env.example
