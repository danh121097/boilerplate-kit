# Agent Instructions

**Project**: Nuxt 4 + TypeScript SSR starter — `app/` srcDir, Pinia, TanStack Vue
Query, Reka UI + Tailwind v4, SSR-guarded axios service layer with JWT bearer +
httpOnly refresh-token rotation and HMAC-signed requests (secret via
`runtimeConfig`), Socket.IO, @nuxtjs/i18n. Package manager: **pnpm**.

Project-specific guidance lives in [`CLAUDE.md`](./CLAUDE.md) and [`docs/`](./docs/README.md).
