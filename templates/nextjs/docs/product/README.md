# Product Docs

This directory holds product contract files by domain.

No domain files yet. When a user provides a project spec, derive product
contract files here — e.g. `overview.md`, `auth.md`, `users.md`.

Do not create domain files before the spec. Empty structure is healthier than
fake product truth.
